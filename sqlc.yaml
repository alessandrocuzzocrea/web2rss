version: "2"
sql:
  - engine: sqlite
    queries: db/queries
    schema: db/migrations
    database:
      uri: sqlite://data/web2rss.sqlite3
    gen:
      go:
        package: db
        out: internal/db
        emit_json_tags: true
